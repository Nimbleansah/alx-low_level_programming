#include "main.h"
/**
 * print_alphabet - print all alphabet in lowercase
 * By Basily
 */
void print_alphabet(void)
{
	char letter;

	for (letter = 'h'; letter <= 'z'; letter++)
		_putchar(letter);

	_putchar('\n');
}
